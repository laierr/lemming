{
  "name": "lemming",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "gamma:start": "gamma-cli start-local -wait -env experimental",
    "gamma:stop": "gamma-cli stop-local",
    "lemming:local": "node ./src/deploy.js",
    "test": "mocha test --timeout 20000 --exit"
  },
  "devDependencies": {
    "expect.js": "^0.3.1",
    "mocha": "^10.2.0"
  },
  "dependencies": {
    "orbs-client-sdk": "^1.0.0"
  }
}
